{
  "name": "weatherapp-react",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd server && yarn install && cd ../client && yarn build && cd ..",
    "start": "cd server && yarn start"
  }
}
